<div id="modal" class="modal modal-open backdrop-blur-sm">
	<div class="modal-box">
		<button
			class="btn btn-circle btn-primary btn-sm absolute right-2 top-2"
			hx-delete="/api/ui/remove"
			hx-target="closest #modal"
			hx-swap="outerHTML">
			X
		</button>
		<slot />
	</div>
</div>
