---
import { t } from "@lib/services/i18n";

const language = Astro.locals.language;
---

<div class="flex flex-col gap-4 p-8 text-center">
	<span>{t("register_to_get_advantages_1", language)}</span>
	<a href="/registration" class="btn btn-warning">{t("register_to_get_advantages_2", language)}</a>
	<a href="/basic-vs-pro" class="btn btn-primary">{t("register_to_get_advantages_3", language)}</a>
</div>
