---
import { getFavoriteDealersCount } from "@lib/services/user";

export const partial = true;
const user = Astro.locals.user;
const count = user.id ? getFavoriteDealersCount(user.id) : 0;
---

{count}
